const INITIAL_TURN = 1;
const MAX_TURNS = 3;

const getDirectWinnerIfAny = () => {};
const getCalculatedWinner = () => {};
const getNextPlayer = () => {};

const playTurn = (game, score, realValue = false) => {};

const Games01Service = {
    playTurn,
};

export default Games01Service;
